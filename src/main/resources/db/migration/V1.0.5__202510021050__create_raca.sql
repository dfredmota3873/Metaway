CREATE TABLE metaway.raca (         id                   uuid NOT NULL,
                                    descricao            varchar(200) NOT NULL,
                                    CONSTRAINT raca_pkey PRIMARY KEY (id));
